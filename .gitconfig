[user]
    name = Radar
    email = radar@protonmail.com
    signingkey = A35E131A4FE7073F
[core]
    pager = cat
    excludesfile = ~/.gitignore
[commit]
    gpgsign = true
[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    bra = branch --all
    diffs = diff --staged
    lg = log --reverse --date=iso --pretty=tformat:'%Cred%h%Creset | %Cgreen%ad %C(blue)%an%Creset%C(yellow)%d%Creset | %s'
    lgs = log --date=iso --pretty=tformat:'%Cred%h%Creset | %Cgreen%ad %C(blue)%an <%ae>%Creset%C(yellow)%d%Creset | %s' --graph
    sh = show --date=iso --pretty=tformat:'%Cred%h%Creset | %Cgreen%ad %C(blue)%an <%ae>%Creset%C(yellow)%d%Creset | %s'
    tags = for-each-ref --format '%(refname:lstrip=2) %09 %(taggerdate) %09 %(taggername) %(taggeremail) %09 %(subject)' refs/tags --sort=version:refname
[color]
    status = auto
    branch = auto
    diff = auto
    ui = auto
[color "grep"]
    match = cyan bold
[color "branch"]
    current = yellow bold
    local = green bold
    remote = cyan bold
[color "diff"]
    meta = bold
    frag = bold
    old = red bold
    new = green bold
    whitespace = red reverse
[color "status"]
    added = green bold
    changed = yellow bold
    untracked = red bold
